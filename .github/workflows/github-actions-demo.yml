name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - name: Setup Go Environment
        uses: actions/setup-go@v2
        with:
          go-version: 1.19.2
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
      - name: Print CWD
        run: echo "$PWD"
#       - name: Git Clone microshift repo
#         uses: sudosubin/git-clone-action@v1.0.1
#         with:
#           repository: openshift/microshift
#           platform: github
#           path: microshift
#       - name: List the contents of cloned repository
#         run: ls /home/runner/work/microshift
#       - name: Run make on the cloned repo
#         run: make /home/runner/work/microshift
#       - name: List the contents of cloned repository
#         run: ls /home/runner/work/microshift/_output/bin
      
          
